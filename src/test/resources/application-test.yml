app:
  security:
    jwt:
      secret: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
    integration-test-disable-jwt: true
  openai:
    enabled: true
    api-key: test-openai-key
    model: gpt-4o-mini
    base-url: https://api.openai.com
    assistant-linguage-id: asst-test-linguage

spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:h2:mem:ms_account_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
